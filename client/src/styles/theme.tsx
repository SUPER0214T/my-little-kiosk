import { Theme } from '@emotion/react';

const theme: Theme = {
  primaryLight: '#FFE9D0',
  primary: '#DB8A4D',
  primaryDark: '#8D603F',
  white: '#fff',
  black: 'black',
};

export default theme;
